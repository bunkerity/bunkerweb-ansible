---
- name: Test Task Selection
  hosts: all
  vars_files:
    - ../defaults/main.yml
  tasks:
    - name: Debian
      include_tasks: ../tasks/centos.yml
      when: ansible_facts['distribution'] == 'CentOS'
